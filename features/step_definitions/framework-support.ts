/**
 * Framework Support File
 * Sets default timeout for all scenarios
 */
import { setDefaultTimeout } from '@cucumber/cucumber';

// Set default timeout to 60 seconds for all steps
setDefaultTimeout(60000);

